apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: config-service
    visualize: "true"
  name: config-service
spec:
  replicas: 4
  template:
    metadata:
      labels:
        app: config-service
        tier: backend
        track: stable
        run: config-service
        visualize: "true"
    spec:
      containers:
      - image: rpicloud/demo-config-service:dns-0.0.1
        name: config-service
        livenessProbe:
          httpGet:
            # Path to probe; should be cheap, but representative of typical behavior
            path: /
            port: 80
          initialDelaySeconds: 120
          timeoutSeconds: 1
